apiVersion: apps/v1
kind: Deployment
metadata:
  name: analysis-back
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analysis-back
  template:
    metadata:
      labels:
        app: analysis-back
    spec:
      # initContainers:
      #   - name: wait-for-surveyanswer
      #     image: busybox
      #     command: ['sh', '-c', 'until nslookup surveyanswer-service; do echo waiting for surveyanswer; sleep 2; done;']
      containers:
        - name: analysis-back
          image: kjk7212/analysis-back:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              protocol: TCP
        - name: redis-container
          image: redis:latest
          ports:
            - containerPort: 6379

---
apiVersion: v1
kind: Service
metadata:
  name: analysis-back-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: analysis-back